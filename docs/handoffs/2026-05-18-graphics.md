# Agent handoff — Graphics / assets reuse (2026-05-18)

> **Role:** Graphics agent — **reuse existing assets only.** Do not add new packs, new buildings, or new enemies. Wire what we already have into `RunScene` and menus where it improves clarity. New content art comes later.

> **Gameplay handoff (previous session):** [2026-05-18.md](./2026-05-18.md)

**Repo:** https://github.com/mateuszBetlej4/mergehold-td.git  
**Branch:** `main`  
**Workspace:** `C:\Users\mateuszb\Documents\AI Projects\TD`  
**Local dev:** `npm install` → `npm run dev` → http://127.0.0.1:5173/  
**Canvas:** 390×694 (`src/game/config.ts`) — mobile portrait, Phaser `FIT` inside React play shell

---

## Your mission (read first)

1. **Audit → wire → polish** using files already in `public/assets/`.
2. **Primary file:** `src/game/scenes/RunScene.ts` (`preload`, `drawMap`, entity sprites, projectiles).
3. **Secondary:** `src/app/PlayHud.tsx` (build dock chip images already point at `/assets/optimized/sprites/`).
4. **Out of scope:** React Collection/Home card art (letter tokens in `App.tsx`), new Quaternius/Mixamo downloads, SFX, UI skin packs, Supabase, gameplay balance.
5. **Source pack:** Kenney Tower Defense (Top-Down) is in repo under `public/assets/source/kenney/` — use only if you need a better crop from an **existing** PNG; prefer `optimized/sprites` first.
6. Update `public/assets/licenses/ASSET_CREDITS.md` only if you swap in additional Kenney files from source.

---

## Architecture (do not restart)

| Layer | Location | Graphics relevance |
|-------|----------|-------------------|
| Run gameplay | `src/game/scenes/RunScene.ts` | **Main work** — preload, map, towers, enemies, structures, fort, hero, troops |
| Run HUD (React) | `src/app/PlayHud.tsx`, `PlayHud.css` | Tower/support thumbnails; paths must match Phaser keys |
| Game mount | `src/game/GameCanvas.tsx`, `config.ts` | Canvas size, background color |
| Data (ids, colors) | `src/data/buildings.ts`, `enemies.ts`, `heroes.ts`, `maps.ts`, `troops.ts` | Drives tints, unlock waves, which entities exist |
| Bridge | `src/game/gameBridge.ts` | `RunUiState` exposes `sprite` id per tower/structure for React dock |
| Menus | `src/app/App.tsx` | **No runtime sprites today** — colored letter `card-token` only |
| Credits | `public/assets/licenses/ASSET_CREDITS.md` | Kenney CC0 + project SVGs |
| Pipeline doc | `docs/ASSET_PIPELINE.md` | Future Quaternius/Mixamo — **not for this pass** |

---

## Asset inventory audit

### A. Runtime folder — `public/assets/optimized/sprites/` (use these first)

| File | Type | Wired in Run? | Wired in PlayHud? | Notes |
|------|------|---------------|-------------------|-------|
| `kenney-tower-archer.png` | PNG | Yes — towers + projectiles context | Yes — dock | Phaser key `kenney-tower-archer` |
| `kenney-tower-cannon.png` | PNG | Yes | Yes | Key `kenney-tower-cannon` |
| `kenney-tower-magic.png` | PNG | Yes | Yes | Key `kenney-tower-magic` |
| `kenney-enemy-grunt.png` | PNG | Yes | — | grunt, **bomber** (reuse) |
| `kenney-enemy-runner.png` | PNG | Yes | — | runner, **bat** (reuse) |
| `kenney-enemy-tank.png` | PNG | Yes | — | tank |
| `kenney-enemy-shield.png` | PNG | Yes | — | shield |
| `kenney-enemy-boss.png` | PNG | Yes | — | gatebreaker boss |
| `kenney-projectile.png` | PNG | Yes | — | All tower shots |
| `kenney-tree.png` | PNG | Yes | — | 4 corners, tinted `palette.hud` |
| `fort.svg` | SVG | Yes | — | Fort at path end |
| `hero-guardian.svg` | SVG | Yes | — | Hero on field **and** all friendly troops |
| `spike-trap.svg` | SVG | Yes | Yes (support tab) | Trap pads + placed traps |
| `barracks.svg` | SVG | Yes | Yes | Placed barracks |
| `coin-mill.svg` | SVG | Yes | Yes | Placed mills |
| `stone-wall.svg` | SVG | Yes | Yes | Placed walls |
| `healing-shrine.svg` | SVG | Yes | Yes | Placed shrines |
| `tower-archer.svg` | SVG | **No** | — | Duplicate of tower art; Kenney PNG used instead |
| `tower-cannon.svg` | SVG | **No** | — | Same |
| `tower-magic.svg` | SVG | **No** | — | Same |
| `enemy-grunt.svg` | SVG | **No** | — | Kenney PNG used instead |
| `enemy-runner.svg` | SVG | **No** | — | Same |
| `enemy-tank.svg` | SVG | **No** | — | Same |
| `enemy-shield.svg` | SVG | **No** | — | Same |
| `enemy-boss.svg` | SVG | **No** | — | Same |
| `projectile-arrow.svg` | SVG | **No** | — | Could replace `kenney-projectile` for archer-only |
| `tree.svg` | SVG | **No** | — | `kenney-tree.png` used instead |
| `kenney-grass.png` | PNG | **No** | — | Ground is flat `palette.ground` rectangle |
| `kenney-path-dot.png` | PNG | **No** | — | Path is vector stroke, not tiles |

### B. Source pack — `public/assets/source/kenney/tower-defense-top-down/`

- Full Kenney **Tower Defense (Top-Down)** kit (CC0): hundreds of PNGs under `PNG/Default size/`, plus `towerDefense_vector.svg`, previews, `License.txt`.
- **Use for:** picking alternate crops, path tiles, grass filler, extra tower/enemy variants — still Kenney, still CC0.
- **Do not** commit entire unoptimized pack into runtime unless you copy a few files into `optimized/sprites/` and document in `ASSET_CREDITS.md`.
- Zip also present: `tower-defense-top-down.zip` (duplicate of extracted folder).

### C. Not in repo yet (pipeline doc only — do not download this task)

- Quaternius enemy/troop packs (`docs/ASSET_PIPELINE.md`)
- UI pack / game-icons / SFX

---

## Game content → asset mapping (existing data only)

### Buildings (`src/data/buildings.ts`) — all 8 in-run

| id | Role | In-run sprite | Phaser / React path |
|----|------|---------------|---------------------|
| `archer-tower` | tower | `kenney-tower-archer.png` | `towerAssetKeys` → preload `kenney-tower-archer` |
| `cannon-tower` | tower | `kenney-tower-cannon.png` | same pattern |
| `magic-tower` | tower | `kenney-tower-magic.png` | same pattern |
| `spike-trap` | trap | `spike-trap.svg` | preload `spike-trap` |
| `barracks` | spawner | `barracks.svg` | preload `barracks` |
| `coin-mill` | economy | `coin-mill.svg` | preload `coin-mill` |
| `stone-wall` | wall | `stone-wall.svg` | preload `stone-wall` |
| `healing-shrine` | support | `healing-shrine.svg` | preload `healing-shrine` |

Tower picker + placed towers use **Kenney PNG**. Support dock uses **SVG** filenames without `kenney-` prefix.

### Enemies (`src/data/enemies.ts`) — 7 definitions, 4 unique Kenney sprites

| id | Wave mix | Runtime texture | Notes |
|----|----------|-----------------|-------|
| `grunt` | W1+ | `kenney-enemy-grunt` | |
| `runner` | W2+ | `kenney-enemy-runner` | |
| `tank` | W4+ | `kenney-enemy-tank` | |
| `shield` | W4+ | `kenney-enemy-shield` | |
| `bat` | data only | **runner** (reuse) | Not in default wave mix |
| `bomber` | data only | **grunt** (reuse) | Not in default wave mix |
| `gatebreaker` | every 5 waves | `kenney-enemy-boss` | Boss scale ~0.58 |

Unused SVGs in `optimized/sprites/enemy-*.svg` could differentiate bat/bomber later **without new downloads**.

### Heroes (`src/data/heroes.ts`) — 3 heroes, 1 sprite

| id | Role | Asset today | Gap |
|----|------|-------------|-----|
| `stone-warden` | guardian | `hero-guardian.svg` + tint `heroTint` | Default selected |
| `wild-arrow` | ranger | **Same** `hero-guardian.svg` | Only color/tint differs (`applyLoadout`) |
| `ember-sage` | mage | **Same** `hero-guardian.svg` | Only color/tint differs |

**Reuse option:** `hero-guardian.svg` + strong tint, or extract 2 silhouettes from Kenney source pack if present — still no new external packs.

### Maps (`src/data/maps.ts`) — 4 maps, procedural only

| id | Theme | Visual today |
|----|-------|----------------|
| `greenwatch` | grass | `palette.ground` / `palette.path` / trees |
| `sunspire` | desert | Same engine — colors from hex in `maps.ts` |
| `frostgate` | snow | Same |
| `underkeep` | dungeon | Same |

**No map tile art.** Reuse options: tint Kenney `kenney-grass.png`, path tiles from source PNG folder, or keep flat colors (clearest scope).

### Troops (`src/data/troops.ts`) — 3 tiers, one graphic

All barracks troops use **`hero-guardian.svg`** tinted `barracksDefinition.color` — no unique troop sprites.

---

## RunScene — where graphics are applied

### `preload()` (lines ~194–211)

Loads 17 textures. Anything placed in-run must be preloaded here (or via React `<img>` for HUD only).

### `drawMap()` (~250–268)

- Full-screen `rectangle` — `palette.ground` (from selected map).
- Path — **Phaser graphics** stroke (44px + 4px outline), not Kenney path tiles.
- **4×** `kenney-tree` — corner decoration, tinted HUD color.
- **Fort** — `fort.svg` at (195, 500).

**Not drawn:** grass tile, path dots, build-pad art (circles only), trap-pad art (small circles).

### Entities on field

| Entity | Asset | Scale / notes |
|--------|-------|----------------|
| Towers | Kenney PNG per type | ~0.58 + tier; badge overlay |
| Traps | `spike-trap` SVG | ~0.42 + tier |
| Barracks / mill / wall / shrine | respective SVG | ~0.46 + tier |
| Enemies | Kenney PNG per `enemyAssetKeys` | ~0.46; boss larger |
| Projectiles | `kenney-projectile` | 0.55 scale, all tower types |
| Hero (lane) | `hero-guardian` + `heroTint` | 0.44 scale |
| Troops | `hero-guardian` + barracks tint | Same as hero silhouette |

### React overlay (`PlayHud.tsx`)

- Towers: `/assets/optimized/sprites/${tower.sprite}.png` where `sprite` = `kenney-tower-*` from `publishRunUiState`.
- Support: `/assets/optimized/sprites/${struct.sprite}.svg` — filename without extension in bridge (e.g. `spike-trap`).

**Keep Phaser preload keys and React paths in sync** when renaming files.

---

## Screens outside Run (lower priority)

| Screen | Current art | Reuse opportunity |
|--------|-------------|-------------------|
| Home / Collection / Heroes / Buildings / Enemies | Lucide icons + letter `card-token` | Optional: use `optimized/sprites/*.svg` as `<img>` in `CardsScreen` — no new assets |
| Play | Phaser canvas + `PlayHud` | **Primary** |
| Upgrades / Settings / Deploy | Text + Lucide | Out of scope unless trivial |

---

## Known visual gaps (reuse-only fixes)

1. **Ground/path** — flat color; `kenney-grass.png` and source path PNGs unused.
2. **Duplicate SVG towers/enemies** — sit unused while Kenney PNGs run; could standardize on one format (SVG or PNG) for consistency.
3. **Hero/troop identity** — one guardian SVG for 3 heroes + 3 troop types.
4. **bat / bomber** — share runner/grunt Kenney sprites; SVG variants exist in repo unused.
5. **Projectiles** — single `kenney-projectile` for archer/cannon/magic; `projectile-arrow.svg` unused.
6. **Build/trap pads** — procedural circles; Kenney kit has pad/tile art in source folder if needed.
7. **Map themes** — color-only; no per-map background illustration.

---

## Recommended work order (graphics agent)

1. **RunScene ground/path** — try `kenney-grass` + 1–2 Kenney path tiles from `source/kenney/.../PNG/` (copy winners to `optimized/sprites/`, update credits).
2. **Enemy differentiation** — wire `enemy-*.svg` for bat/bomber OR tint Kenney PNGs differently (no new files).
3. **Hero loadout** — per-hero: same SVG with distinct tint OK; or 3 crops from Kenney characters if found in source.
4. **Troops** — tint `hero-guardian` by role (blocker/ranged/burst) or reuse smallest Kenney unit PNG from source.
5. **Projectiles** — optional `projectile-arrow.svg` for archer; keep Kenney blob for cannon/magic.
6. **Pads** — optional Kenney “tile” from source for `+` / trap slots (subtle, don’t clutter).
7. **Collection screens** — optional pass: building/enemy SVG thumbs in `App.tsx` (still no new downloads).

Do **not** change gameplay constants, wave logic, or HUD layout unless required for art (coordinate with gameplay handoff).

---

## Technical constraints

- **Phaser:** `this.load.image(key, path)` or `this.load.svg(key, path, { width, height })`. Use consistent keys in `towerAssetKeys` / `enemyAssetKeys`.
- **SVG in Phaser:** already used for structures/fort/hero; test mobile performance if adding many SVG instances.
- **Tinting:** `setTint()` / `setTintFill()` used on trees, hero, troops, enemies on hit — preserve for new sprites.
- **Depth:** map ~1–4, entities ~7–18, UI overlay Phaser ~28–49, upgrade overlay ~80, React HUD z-index 2 on top.
- **Scale reference:** towers ~0.58 base, enemies ~0.46, fort ~0.78, trees ~0.6–0.74.

---

## Browser testing (graphics)

- Viewport **390×844** (mobile).
- **Play** → full run: wave 1–2, place each building type, boss wave 5.
- Check: upgrade overlay readable; **top stat bar + bottom dock stay visible** during upgrade (BUG-003 fixed).
- Check: tower upgrade on pad (BUG-005 fixed).
- Pause/speed/home in `PlayHud` — don’t break hit areas when changing art.

Phaser not in a11y tree — screenshot + visual check.

---

## Git workflow

```bash
git status --short --branch
npm run build
# after visual check in browser
git add public/assets/ src/game/scenes/RunScene.ts src/app/PlayHud.tsx public/assets/licenses/ASSET_CREDITS.md
git commit -m "Reuse existing sprites for run map and units"
git push
```

Windows: simple `-m "message"`. No force-push `main`. Don’t edit git config.

---

## Related docs

- `docs/ASSET_PIPELINE.md` — future packs (not this task)
- `docs/tracking/BUG_TRACKER.md` — BUG-003/004/005 UI fixes on `main`
- `docs/tracking/DECISION_LOG.md` — DEC-005 Kenney assets, DEC-008–015 gameplay/UI decisions
- `docs/DEVELOPMENT_CHECKLIST.md` — Assets section: Kenney done; Quaternius/UI/SFX later

---

## Quirks

- `public/assets/source/kenney/` is **large** (~600+ PNGs) — don’t glob into build; copy only what you wire.
- Some `optimized/sprites` files are **duplicates** (SVG + Kenney PNG for same role) — pick one style per category for cohesion.
- `RunScene.ts` is ~1650 lines — only touch `preload`, `drawMap`, spawn/placement visuals, and asset key maps unless necessary.
- React `PlayHud` hides dock grids during upgrade but keeps top bar (see `play-hud--upgrade` in `PlayHud.css`).

---

## Prompt for graphics agent (copy below)

---

You are the **graphics agent** for **Mergehold TD**. **Reuse assets already in the repo only** — no new packs or new game content.

1. Read `docs/handoffs/LATEST.md` → this file (`docs/handoffs/2026-05-18-graphics.md`).
2. Read `public/assets/licenses/ASSET_CREDITS.md`, list `public/assets/optimized/sprites/`, skim `public/assets/source/kenney/tower-defense-top-down/PNG/`.
3. Read `src/game/scenes/RunScene.ts` (`preload`, `drawMap`, `towerAssetKeys`, `enemyAssetKeys`, placement sprites) and `src/app/PlayHud.tsx` (dock image paths).
4. Read `src/data/buildings.ts`, `enemies.ts`, `heroes.ts`, `maps.ts` — wire art for **existing** ids only.
5. Priority: **Run scene** — ground/path, fort, towers, enemies, structures, hero, troops, projectiles. Then optional Collection thumbnails from existing SVG/PNG.
6. Copy any new runtime file from Kenney **source** into `optimized/sprites/`, update `ASSET_CREDITS.md`, preload in Phaser.
7. `npm run build`, browser-test Play at ~390×844, commit, push.
8. Do **not** create a new handoff unless the user asks (see `docs/handoffs/README.md`).

Repo: `C:\Users\mateuszb\Documents\AI Projects\TD` — https://github.com/mateuszBetlej4/mergehold-td.git

---
